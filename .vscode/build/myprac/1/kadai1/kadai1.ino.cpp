// Auto-generated by arduino-build-daemon
#include <Arduino.h>
#line 1 "kadai1.ino"
#define USE_TIMER3_ISR
#include "mono_con.h"

const int edges[9] = { 102, 204, 306, 408, 510, 612, 714, 816, 928 };

int x, y;
int prex, prey;
int xval, yval;

word in;

int getIdx(int n) {
	for (int i = 0; i < 9; i++) {
		if (n < edges[i]) {
			return 9 - i;
		}
	}
	return 0;
}

void onedisp(int n, int m) {
	static int pren = -1, prem = -1;
	if (n != pren || m != prem) {
		pren = n, prem = m;
		disp(num[n], num[m]);
	}
}

ISR(TIMER3_COMPA_vect) {
	if (in++ > 5) {
		in = 0;

		x = analogRead(A1);
		y = analogRead(A2);

		xval = getIdx(x);
		yval = getIdx(y);
	}
}

void setup() {
	config_init();
	serial_init();

	x = analogRead(A1);
	y = analogRead(A2);

	xval = getIdx(x);
	yval = getIdx(y);

	disp(num[xval], num[yval]);
}

void loop() {
	onedisp(xval, yval);
}